export type Product = {
    id: number
    name: string
    price: number
    description: string
    image: string
    category_id: number
}

export type Category = {
    id: number
    name: string
}
